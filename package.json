{
  "name": "trae-agent-web",
  "version": "1.0.0",
  "description": "Web interface for Trae Agent - LLM-based agent for software engineering tasks",
  "scripts": {
    "build": "mkdir -p build/client && cp -r public/* build/client/",
    "dev": "node test-local.js",
    "start": "node test-local.js",
    "test": "node test-local.js",
    "dev:python": "python -m http.server 8000 --directory public",
    "dev:pnpm": "pnpm install && pnpm run build && python -m http.server 8000 --directory build/client",
    "deploy": "wrangler pages deploy build/client",
    "deploy:manual": "pnpm run build && wrangler pages deploy build/client"
  },
  "keywords": [
    "ai",
    "llm",
    "agent",
    "software-engineering",
    "cloudflare",
    "web-interface"
  ],
  "author": "Trae Agent Contributors",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.78.10"
  },
  "packageManager": "pnpm@9.0.0"
}